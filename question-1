package scalaquestions

object ScalaQuestion1 {
   def main(args : Array[String]) : Unit = {
     var l1 = List(102,105,101,108,103)
     println(Listrev(l1))
}
def Listrev[A](s: List[A], d: List[A] = Nil): List[A] = {
if (s == Nil) d else Listrev(s.tail, s.head :: d)
}
}
